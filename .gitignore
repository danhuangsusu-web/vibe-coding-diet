node_modules/
.pnpm-store/
.local/
.next/
dist/
.taro-cache/
coverage/
*.log
.env
.env.local
.env.*.local
!.env.example
apps/server/prisma/dev.db*

# === 环境变量与密钥（核心红线，勿删勿改）===
# 显式重复声明，作为 .env 规则的双保险
apps/server/.env
apps/server/.env.local

# WorkBuddy 私有数据（会话记忆等，绝不入库）
.workbuddy/

# 编译器缓存
.swc/
.swc-cache/

# TypeScript 增量构建缓存
*.tsbuildinfo

# 内部审稿文档：含设计文档缺陷清单，不进公开仓库（本地仍保留供使用）
workflow/docs-fix-prompt.md
